def contains(lst: list, value: int) -> bool:
    if isinstance(value, str):
        raise ValueError("Khong phai so!")
    a = lst.index(value)
    if a == isinstance(value, int):
        return True
    return False